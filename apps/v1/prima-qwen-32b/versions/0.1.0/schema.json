{
  "type": "object",
  "required": [
    "ingress",
    "properties"
  ],
  "properties": {
    "engine": {
      "type": "object",
      "title": "Engine",
      "required": [
        "profile"
      ],
      "properties": {
        "profile": {
          "type": "string",
          "title": "Profile",
          "default": "balanced",
          "enum": [
            "balanced",
            "throughput",
            "latency",
            "power",
            "cost",
            "baseline"
          ]
        }
      }
    },
    "ingress": {
      "type": "object",
      "title": "Ingress",
      "required": [
        "host"
      ],
      "properties": {
        "enabled": {
          "type": "boolean",
          "title": "Enable Ingress",
          "default": true
        },
        "host": {
          "type": "string",
          "title": "Hostname",
          "default": "armada.primalabs.com"
        }
      }
    }
  }
}